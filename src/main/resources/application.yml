server:
  port: 8081

spring:
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
    open-in-view: false
  datasource:
    url: jdbc:postgresql://localhost:5432/vitrix
    username: postgres
    password: 11122006Pl
    driver-class-name: org.postgresql.Driver
  mvc:
    hiddenmethod.filter.enabled: true
  servlet:
    multipart.max-file-size: 5MB